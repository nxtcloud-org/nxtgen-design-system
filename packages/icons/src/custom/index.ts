/**
 * NxtGen 자체 아이콘 (AI 도메인 전용).
 *
 * 추가 방법:
 *   1. src/custom/svg/ 에 SVG 파일 추가 (24×24, stroke 1.5, currentColor)
 *   2. `bun run build:custom` 실행 → src/custom/generated.ts 생성
 *   3. 이 파일에서 re-export
 *
 * 현재는 비어 있음. Phase 1 이후 추가 예정.
 */

export {};
