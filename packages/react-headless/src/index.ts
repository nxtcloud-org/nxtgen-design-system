/**
 * NxtGen Headless — 동작/접근성만. 스타일 0.
 *
 * 현재는 Radix Slot만 노출 (asChild 패턴용).
 * 추후 Radix 다른 primitive들을 도메인 친화적으로 wrapping해서 추가.
 */

export { Slot, Slottable } from "@radix-ui/react-slot";
export type { SlotProps } from "@radix-ui/react-slot";
